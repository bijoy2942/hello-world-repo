name: Hello World
on:
  push:
    branches: [ "Master" ]

jobs:
  hello-job:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@latest

      - name: Echo text
        run: echo "Hello, world"
